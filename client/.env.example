# Client environment variables
# Set these in Vercel → Project Settings → Environment Variables

# Full URL of the deployed Socket.IO server.
# Leave empty to fall back to window.location.origin (local dev only).
SOCKET_SERVER_URL=https://your-quiz-server.example.com
