{
  "name": "concurrent-quiz-server",
  "version": "1.0.0",
  "description": "Socket.IO game server for the Concurrent Quiz",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch --env-file=.env index.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.37.0",
    "express": "^4.21.0",
    "socket.io": "^4.8.0"
  }
}
